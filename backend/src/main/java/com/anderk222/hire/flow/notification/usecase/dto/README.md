# usecase / dto

DTOs del módulo, separados de las entidades de dominio: `command/` para
datos de entrada, `response/` para datos de salida. Son `record` de Java
(inmutables), así que no necesitan Lombok.
