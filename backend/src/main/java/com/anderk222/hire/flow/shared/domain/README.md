# shared / domain

Conceptos de dominio compartidos por todos los módulos: la jerarquía base
de excepciones de negocio (`exception`) y el contrato de evento de dominio
(`event`) que habilita el patrón Observer entre módulos.
