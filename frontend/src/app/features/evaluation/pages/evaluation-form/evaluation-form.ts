import { Component } from '@angular/core';

/** UC-06: evaluator scores criteria and records a recommendation. */
@Component({
  selector: 'app-evaluation-form',
  imports: [],
  templateUrl: './evaluation-form.html',
  styleUrl: './evaluation-form.scss'
})
export class EvaluationForm {
  // TODO: reactive form bound to SubmitEvaluationRequest + EvaluationService.submit(...).
}
