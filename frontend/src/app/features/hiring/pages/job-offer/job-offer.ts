import { Component } from '@angular/core';

/** UC-07: generate a job offer and track the candidate's response. */
@Component({
  selector: 'app-job-offer',
  imports: [],
  templateUrl: './job-offer.html',
  styleUrl: './job-offer.scss'
})
export class JobOfferPage {
  // TODO: form bound to GenerateJobOfferRequest + JobOfferService.generate(...).
}
