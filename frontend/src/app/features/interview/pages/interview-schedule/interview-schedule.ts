import { Component } from '@angular/core';

/** UC-05: recruiter picks date, mode, type and participants for an interview. */
@Component({
  selector: 'app-interview-schedule',
  imports: [],
  templateUrl: './interview-schedule.html',
  styleUrl: './interview-schedule.scss'
})
export class InterviewSchedule {
  // TODO: reactive form bound to ScheduleInterviewRequest + InterviewService.schedule(...).
}
