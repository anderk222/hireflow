# features / vacancy / components

Componentes de presentación usados solo dentro del módulo de vacantes
(p. ej. la tarjeta de resumen mostrada en el listado). Si con el tiempo
otro módulo necesitara reutilizarlos, se promueven a `shared/ui`.
