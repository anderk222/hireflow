# features / vacancy / services

Servicio HTTP hacia `/api/vacancies` (controlador `VacancyController` del
backend). Es la única capa que conoce la URL de la API; los componentes
solo consumen este servicio.
